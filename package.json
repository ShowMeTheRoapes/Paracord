{
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "cd paracord-ui && echo Linting frontend... && yarn lint-fix && cd ../",
      "pre-push": "cd paracord-ui && echo Running frontend tests... && yarn test && cd ../"
    }
  },
  "devDependencies": {
    "husky": "~2.4.1"
  }
}
