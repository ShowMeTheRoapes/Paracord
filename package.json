{
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "cd paracord-ui && echo Linting UI files... && yarn && yarn lint-fix && cd ../ && cd server && echo Formatting server files... && mvn fmt:format && cd../",
      "pre-push": "cd paracord-ui && echo Running UI tests... && yarn && yarn test && cd ../ && cd server && echo Building server project... && mvn clean install && cd../"
    }
  },
  "devDependencies": {
    "husky": "~2.4.1"
  }
}
