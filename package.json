{
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "cd paracord-ui && echo Linting UI files... && yarn lint-fix && cd ../",
      "pre-push": "cd paracord-ui && echo Running UI tests... && yarn test && cd ../"
    }
  },
  "devDependencies": {
    "husky": "~2.4.1"
  }
}
